<div class="card">
  <div class="card-header">
    <h1><%= @relationship.identifier_name %></h1>
  </div>
  <div class="card-body">
  </div>
  <div class="card-footer">
    <%= link_to 'Edit', edit_relationship_path(@relationship), class: 'btn btn-warning' %>
    <%= link_to 'Delete', @relationship, class: 'btn btn-danger', data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' } %>
  </div>
</div>

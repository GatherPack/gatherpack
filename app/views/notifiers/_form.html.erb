<%= f.error_notification %>
<%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

<div class="form-inputs">
  <%= f.input :person_gid, as: :combobox, scope: "people" %>

  <%= f.input :schedule_type, as: :select, collection: Notifier::SCHEUDLE_TYPES %>

  <%= f.input :scope %>
  
  <%= f.input :target %>
</div>

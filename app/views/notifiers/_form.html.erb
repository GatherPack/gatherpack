<%= f.error_notification %>
<%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

<div class="form-inputs">
  <%= f.input :person_id, as: :searching_combobox, scope: "people" %>

  <%= f.input :schedule_type, as: :select, collection: Notifier::SCHEDULE_TYPES %>

  <%= f.input :scope %>
  
  <%= f.input :target %>
</div>

<%# Link to the "Next" page
  - available local variables
    url:           url to the next page
    current_page:  a page object for the currently displayed page
    total_pages:   total number of pages
    per_page:      number of items to fetch per page
    remote:        data-remote
-%>
<span class="next mx-2 d-none d-sm-block">
  <%= link_to t('views.pagination.next').html_safe, url, rel: 'next', remote: remote, class: ['btn', 'btn-primary', current_page.last? ? 'disabled' : ''], disabled: current_page.last? %>
</span>
<span class="next mx-2 d-block d-sm-none">
  <%= link_to "›", url, rel: 'next', remote: remote, class: ['btn', 'btn-primary', current_page.last? ? 'disabled' : ''], disabled: current_page.last? %>
</span>

<%= simple_form_for(@variable) do |f| %>
<div class="card">
  <div class="card-header">
    <h1>Editing <%= @variable.identifier_name %></h1>
  </div>
  <div class="card-body">
    <%= render "form", f: f %>
  </div>
  <div class="card-footer">
    <%= link_to "Cancel", @variable, class: 'btn btn-warning' %>
    <%= f.button :submit, class: 'btn btn-primary' %>
  </div>
</div>
<% end %>

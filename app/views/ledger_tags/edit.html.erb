<% breadcrumb @ledger_tag %>

<%= simple_form_for(@ledger_tag) do |f| %>
<div class="card">
  <div class="card-header">
    <h1>Editing <%= @ledger_tag.identifier_name %></h1>
  </div>
  <div class="card-body">
    <%= render "form", f: f %>
  </div>
  <div class="card-footer">
    <%= link_to "Cancel", @ledger_tag, class: 'btn btn-warning' %>
    <%= f.button :submit, class: 'btn btn-primary' %>
  </div>
</div>
<% end %>

<%= f.error_notification %>
<%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

<div class="form-inputs">
  <%= f.input :value %>
  <%= label_tag "token[tokenable_gid]", "Assign to" %>
  <%= combobox_tag "token[tokenable_gid]", combo_search_path(scope: "people hooks"), render_in: { partial: 'search/result' } %>
</div>

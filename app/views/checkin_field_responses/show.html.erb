<% breadcrumb @checkin_field_response %>

<div class="card">
  <div class="card-header">
    <h1><%= @checkin_field_response.identifier_name %></h1>
  </div>
  <div class="card-body">
  </div>
  <div class="card-footer">
    <%= link_to 'Edit', edit_checkin_field_response_path(@checkin_field_response), class: 'btn btn-warning' %>
    <%= link_to 'Delete', @checkin_field_response, class: 'btn btn-danger', data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' } %>
  </div>
</div>

<%= f.error_notification %>
<%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

<div class="form-inputs">
  <%= f.association :checkin %>
  <%= f.association :checkin_field %>
  <%= f.input :response %>
</div>

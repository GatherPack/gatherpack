<% breadcrumb @team_type %>

<%= simple_form_for(@team_type) do |f| %>
<div class="card">
  <div class="card-header">
    <h1>Editing <%= @team_type.identifier_name %></h1>
  </div>
  <div class="card-body">
    <%= render "form", f: f %>
  </div>
  <div class="card-footer">
    <%= link_to "Cancel", @team_type, class: 'btn btn-warning' %>
    <%= f.button :submit, class: 'btn btn-primary' %>
  </div>
</div>
<% end %>

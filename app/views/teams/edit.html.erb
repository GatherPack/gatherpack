<% breadcrumb @team %>

<%= simple_form_for(@team) do |f| %>
<div class="card">
  <div class="card-header">
    <h1>Editing <%= @team.identifier_name %></h1>
  </div>
  <div class="card-body">
    <%= render "form", f: f %>
  </div>
  <div class="card-footer">
    <%= link_to "Cancel", @team, class: 'btn btn-warning' %>
    <%= f.button :submit, class: 'btn btn-primary' %>
  </div>
</div>
<% end %>

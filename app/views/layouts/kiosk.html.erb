<!DOCTYPE html>
<html class="kiosk-layout">
  <head>
    <title> <%= Settings[:title] %> | <%= content_for(:title) %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <% if (favicon = Theme.instance.favicon_32) %>
      <link rel="icon" href="<%= url_for(favicon) %>" type="image/png">
    <% else %>
      <link rel="icon" href="/apple-touch-icon.png" type="image/png">
    <% end %>
    <% if (apple_touch = Theme.instance.apple_touch_icon) %>
      <link rel="apple-touch-icon" href="<%= url_for(apple_touch) %>">
    <% else %>
      <link rel="apple-touch-icon" href="/apple-touch-icon.png">
    <% end %>
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Bubblegum+Sans&family=Fira+Code:wght@300..700&family=Fira+Sans:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap" rel="stylesheet">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= render "layouts/theme_styles" %>
    <%= javascript_importmap_tags %>
    <%= font_awesome_tag %>
  </head>

  <body>
    <header class="navbar navbar-expand-lg bg-dark">
      <div class="container-fluid">
        <a class="navbar-brand" href="/">
          <% if Theme.instance.logo.attached? %>
            <%= image_tag Theme.instance.logo, alt: Settings[:title] %>
          <% else %>
            <%= image_tag "logo.svg" %>
          <% end %>
          <%= Settings[:title] %>
        </a>
      </div>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0 d-flex align-items-center">
        <% if current_user != true_user %>
          <div class="nav-item d-flex align-items-center px-1 lh-1 text-white text-end">
            <%= true_user.person.identifier_name %>
          </div>
          <div class="nav-item d-flex align-items-center px-1">
            <%= image_tag(get_avatar_url(true_user.person.avatar), class: "person-img", style: "height: 2em; width: auto;") %>
          </div>
          <div class="nav-item px-1 text-center w-100 text-white">
            impersonating
          </div>
        <% end %>
        <div class="nav-item d-flex align-items-center px-1 lh-1 text-white text-end">
          <%= current_user.person.identifier_name %>
        </div>
        <div class="nav-item d-flex align-items-center px-1 pe-3">
          <%= image_tag(get_avatar_url(current_user.person.avatar), class: "person-img", style: "height: 2em; width: auto;") %>
        </div>
      </ul>
    </div>
    </header>

    <div class="kiosk-container">
      <div class="row">
        <main>
          <%= yield %>
        </main>
      </div>
    </div>
  </body>
</html>

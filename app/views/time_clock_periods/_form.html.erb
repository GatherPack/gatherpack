<%= f.error_notification %>
<%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

<div class="form-inputs">
  <%= f.input :name %>
  <%= f.input :start_time, as: :string, input_html: { type: :date } %>
  <%= f.input :end_time, as: :string, input_html: { type: :date } %>
  <%= f.input :permission, as: :select, collection: @permissions_keys, label_method: :titleize, include_blank: false %>
  <%= f.input :team_id, as: :searching_combobox, label: 'Team', scope: "managed_teams", include_blank: admin? %>
</div>

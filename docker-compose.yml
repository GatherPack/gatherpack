services:
  db:
    image: postgres:16
    environment: 
      POSTGRES_PASSWORD: 'password'
    volumes:
      - ./tmp/db:/var/lib/postgresql/data
    ports:
      - 5432:5432
  
  mailcatcher:
    image: schickling/mailcatcher
    ports:
      - '1080:1080'
      - '1025:1025'
